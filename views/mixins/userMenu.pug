mixin userMenu()
    .dropdown
        .dropbtn
                a(href='/') Home
                a(href=`/${user.username}`) My Profile
                a(href='/account') Settings
                a(href='/logout') Logout
